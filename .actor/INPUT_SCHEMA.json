{
  "title": "YouTube Scraper Input",
  "description": "Configuration for YouTube video search scraping",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "keywords": {
      "title": "Search Keywords",
      "type": "array",
      "description": "Keywords to search for on YouTube",
      "items": { "type": "string" },
      "editor": "stringList"
    },
    "maxVideos": {
      "title": "Maximum Videos",
      "type": "integer",
      "description": "Maximum number of videos to scrape",
      "default": 30,
      "minimum": 1,
      "maximum": 500
    },
    "sortBy": {
      "title": "Sort By",
      "type": "string",
      "description": "Sort order for search results",
      "enum": ["relevance", "date", "viewCount"],
      "default": "relevance"
    }
  },
  "required": ["keywords"]
}